/*
 * HomePage Messages
 *
 * This contains all the text for the HomePage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.SideMenu';

export default defineMessages({
  home: {
    id: `${scope}.home`,
  },
  wallet: {
    id: `${scope}.wallet`,
  },
  market: {
    id: `${scope}.market`,
  },
  eth_wallet: {
    id: `${scope}.eth_wallet`,
  },
  your_pad: {
    id: `${scope}.your_pad`,
  },
  your_buds: {
    id: `${scope}.your_buds`,
  },
  tokenomics: {
    id: `${scope}.tokenomics`,
  },
});
