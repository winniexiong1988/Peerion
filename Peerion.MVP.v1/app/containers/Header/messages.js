/*
 * HomePage Messages
 *
 * This contains all the text for the HomePage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.Header.dashboard';

export default defineMessages({
  home: {
    id: `${scope}.home`,
  },
  market: {
    id: `${scope}.market`,
  },
  wallet: {
    id: `${scope}.wallet`,
  },
  your_pad: {
    id: `${scope}.your_pad`,
  },
  your_buds: {
    id: `${scope}.your_buds`,
  },
  tokenomics: {
    id: `${scope}.tokenomics`,
  },
});
